refactor(auth): complete migration from JSON-file to SQLite service layer

Replace the half-finished auth refactoring with a fully consistent
SQLite-backed implementation:

- sessionService: fix createSession signature to (user, userAgent, ip),
  persist sessions via userRepository.updateSessions after every mutation
- authService: fix createSession call sites, implement setupTwoFactor and
  verifyTwoFactor with tempSecret expiry, add updateProfile, fix recovery
  code check to also try totpCode field for backwards compatibility
- auth.js: full rewrite removing all readDb/writeDb usage; all 17 routes
  now delegate to authService / userRepository / familyRepository;
  duplicate export/import block removed; reauth migrated to async bcrypt
- tests: migrate twoFactorAuth and authExtended test DB manipulation from
  readDb/writeDb to userRepository / SQLite direct access

Quality gate: lint 0/0, 223/223 tests passed, build ok, scan:code ok
